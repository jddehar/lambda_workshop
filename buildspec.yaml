version: 0.1
phases:
  install:
    commands:
      - npm install
      - echo ${BUCKET}
      - ./node_modules/typescript/bin/tsc
      - sam package --output-template-file packaged.yaml --s3-bucket ${BUCKET}
artifacts:
  type: zip
  files:
    - template.yaml
    - outputtemplate.yaml